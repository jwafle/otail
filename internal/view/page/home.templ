package page

import (
	"github.com/jwafle/otail/internal/view/layout"
	"github.com/jwafle/otail/internal/view/message"
	"strconv"
)

templ Home(json string) {
	@layout.Base() {
		<div>
			for i := 0; i < 20; i++ {
				@message.Message(strconv.Itoa(i), json)
			}
		</div>
	}
}
